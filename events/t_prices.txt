namespace = t_prices

country_event = {
	id = t_prices.1
	title = t_prices.1.title
	desc = t_prices.1.desc
	picture = TRADE_GOODS_PLANTATION_GOODS_IN_WAREHOUSE_eventPicture

	fire_only_once = yes
	
	major = yes

	trigger = {
		NOT = { has_global_flag = t_slave_trade_surged }
		colony = 2
		slaves = 5
		
	}
	mean_time_to_happen = {
		months = 1
	}
	
	
	option = {
		name = t_prices.1.a
		change_price = {
			trade_goods = slaves
			key = T_SLAVE_TRADE_SURGED
			value = 0.3
			duration = -1
		}
		add_years_of_income  = 0.25
		add_prestige = 20
		set_global_flag = t_slave_trade_surged
	}
}
